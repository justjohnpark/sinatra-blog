<h1><%= @author.first_name %></h1>

<ul>
  <% @entries.each do |entry| %>
    <li><a href='/entries/<%= entry.id %>'><%= entry.title %></a> <small><%= timestamp_to_s(entry.created_at) %></small></li>
  <% end %>
</ul>